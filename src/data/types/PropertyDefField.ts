/** Property-specific wire indices, starting at 0x03 (port of C# `PropertyDefField`). */
export enum PropertyDefField {
  ValueType = 0x03,
  OrderingControl = 0x04,
  HistoryControl = 0x05,
  DefaultValue = 0x06,
}
