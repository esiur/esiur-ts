/** Event-specific wire indices, starting at 0x03 (port of C# `EventDefField`). */
export enum EventDefField {
  ArgumentType = 0x03,
  ArgumentName = 0x04,
  OrderingControl = 0x05,
  HistoryControl = 0x06,
}
